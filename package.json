{
  "name": "hellsec.dev",
  "version": "1.0.0",
  "description": "Static website for HellSec (product R&D and cybersecurity)",
  "scripts": {
    "test": "echo \"No tests defined for static site\" && exit 0",
    "build": "node build.mjs",
    "validate:html": "node scripts/validate-html.mjs"
  },
  "private": true,
  "devDependencies": {
    "esbuild": "^0.21.5",
    "html-minifier-terser": "^7.2.0",
    "html-validator": "^4.1.1",
    "html-validator-cli": "^6.0.0"
  }
}
