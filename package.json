{
  "name": "pumalabs.io",
  "version": "1.0.0",
  "description": "Static website for Puma Labs (product R&D and cybersecurity)",
  "scripts": {
    "test": "echo \"No tests defined for static site\" && exit 0",
    "build": "node build.mjs",
    "validate:html": "npx html-validator-cli --path dist --quiet"
  },
  "private": true,
  "devDependencies": {
    "esbuild": "^0.21.5",
    "html-minifier-terser": "^7.2.0",
    "html-validator-cli": "^6.0.0"
  }
}
